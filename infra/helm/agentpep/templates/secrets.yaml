apiVersion: v1
kind: Secret
metadata:
  name: {{ include "agentpep.fullname" . }}-secrets
  labels:
    {{- include "agentpep.labels" . | nindent 4 }}
type: Opaque
data:
  # WARNING: This is a placeholder value (decodes to "CHANGE_ME").
  # You MUST replace this before deploying to any shared or production environment.
  # Use sealed-secrets, external-secrets, or --set to inject real credentials.
  api-key: Q0hBTkdFX01F
