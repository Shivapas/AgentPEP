apiVersion: v1
kind: Secret
metadata:
  name: {{ include "agentpep.fullname" . }}-secrets
  labels:
    {{- include "agentpep.labels" . | nindent 4 }}
type: Opaque
data:
  # Base64-encoded placeholder — replace via sealed-secrets or external-secrets
  api-key: Q0hBTkdFX01F
